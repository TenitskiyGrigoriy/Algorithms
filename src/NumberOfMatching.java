/**
 * Two lists of numbers are given, which can contain up to 100,000 numbers each.
 * Count how many numbers are contained simultaneously in both the first list and the second.
 *
 * Note. This Python problem can be solved in one line.
 *
 * Input data
 * Two lists of numbers are entered. All the numbers in each list are on a separate line.
 *
 * Output data
 * Print the answer to the problem.
 */

public class NumberOfMatching {
}
