/**
 * Two lists of numbers are given, which can contain up to 10,000 numbers each.
 * Print all the numbers that are included in both the first and second list in ascending order.
 *
 * Note. And even this Python problem can be solved in one line.
 *
 * Input data
 * Two lists of integers are entered. All the numbers in each list are on a separate line.
 *
 * Output data
 * Print the answer to the problem.
 */

public class IntersectionOfSets {
}
